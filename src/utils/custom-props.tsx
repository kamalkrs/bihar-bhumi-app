import { ViewProps } from "react-native";

export interface CustomProps extends ViewProps{
    m: number,
    mb: number,
    ms: number,
    me: number,
    mt: number,
    mx: number,
    my: number
    p: number,
    pt: number, 
    pe: number,
    pb: number,
    ps: number,
    px: number,
    py: number
}